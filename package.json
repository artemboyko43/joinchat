{
  "name": "joinchat",
  "description": "example chat",
  "version": "0.0.1",
  "dependencies": {
  },
  "engines": {
    "node": "6.11.1"
  },
  "scripts": {
    "heroku-prebuild": "cd server && node peer-server.js",
    "heroku-postbuild": "cd public && node website-server.js"
  }
}